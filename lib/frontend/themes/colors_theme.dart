import 'package:flutter/material.dart';

import 'material_color.dart';

abstract class ColorThemes{
  static final primarySwatch = createMaterialColor(const Color(0xFFE30613));
  static final black = createMaterialColor(const Color(0xFF000000));
}